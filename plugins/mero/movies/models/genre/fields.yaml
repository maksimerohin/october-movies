fields:
    title:
        label: 'mero.movies::lang.movies.title'
        span: auto
        type: text
    slug:
        label: 'mero.movies::lang.movies.slug'
        span: auto
        preset:
            field: title
            type: slug
        type: text
